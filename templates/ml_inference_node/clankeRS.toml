[project]
name = "{{PROJECT_NAME}}"

[node]
name = "{{PROJECT_NAME}}"

[model.policy]
source_framework = "pytorch"
path = "models/policy.onnx"
backend = "onnxruntime"
device = "cpu"

[model.policy.input]
shape = [1, 10]
layout = "nchw"
dtype = "f32"
